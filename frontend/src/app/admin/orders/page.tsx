'use client';

import { useCallback, useEffect, useMemo, useState } from 'react'; import AdminShell from '@/components/admin/AdminShell'; import OrderDrawer from '@/components/admin/OrderDrawer'; import { Icon } from '@/components/admin/Icons'; import { Empty, ErrorState, Skeleton, StatusBadge, Toast } from '@/components/admin/UI'; import { adminFetch } from '@/lib/admin/client'; import { dateTime, money, Order, orderCustomer, orderEmail, orderTotal, Paged } from '@/lib/admin/types';
const statuses=['ALL','PENDING','PAID','PROCESSING','SHIPPED','DELIVERED','CANCELLED','REFUNDED'];
export default function OrdersPage(){const [page,setPage]=useState(1);const [query,setQuery]=useState('');const [status,setStatus]=useState('ALL');const [data,setData]=useState<Paged<Order>|null>(null);const [selected,setSelected]=useState<number|null>(null);const [error,setError]=useState('');const [toast,setToast]=useState<{message:string;kind:'success'|'error'}|null>(null);useEffect(()=>{const requested=new URLSearchParams(window.location.search).get('status');if(requested&&statuses.includes(requested))setStatus(requested)},[]);
const load=useCallback(async()=>{setError('');try{setData(await adminFetch<Paged<Order>>(`orders?page=${page}&limit=25`))}catch(e){setError(e instanceof Error?e.message:'Unable to load orders.')}},[page]);useEffect(()=>{load()},[load]);
const rows=useMemo(()=>data?.data.filter(order=>(status==='ALL'||order.status===status)&&(!query||`${order.id} ${orderCustomer(order)} ${orderEmail(order)} ${order.trackingCode||''}`.toLowerCase().includes(query.toLowerCase())))||[],[data,status,query]);const flash=(message:string,kind:'success'|'error'='success')=>{setToast({message,kind});if(kind==='success')load()};
return <AdminShell title="Orders" eyebrow="Fulfilment"><div className="adm-page-head"><div><h1>Orders</h1><p>Review customer, payment and delivery details, then manage fulfilment.</p></div></div><div className="adm-toolbar"><label className="adm-search"><Icon name="search"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search order, customer, email or tracking…"/></label><select value={status} onChange={e=>setStatus(e.target.value)} aria-label="Status filter">{statuses.map(s=><option key={s} value={s}>{s==='ALL'?'All statuses':s}</option>)}</select></div><section className="adm-panel">{error?<ErrorState message={error} retry={load}/>:!data?<Skeleton/>:!rows.length?<Empty title="No orders found" message="No order on this page matches the selected filters."/>:<><div className="adm-table-wrap"><table className="adm-table"><thead><tr><th>Order</th><th>Customer</th><th>Status</th><th>Total</th><th>Delivery</th><th>Created</th><th></th></tr></thead><tbody>{rows.map(order=><tr key={order.id} onClick={()=>setSelected(order.id)} style={{cursor:'pointer'}}><td data-label="Order"><strong>#{order.id}</strong></td><td data-label="Customer"><strong>{orderCustomer(order)}</strong><br/><small style={{color:'var(--adm-muted)'}}>{orderEmail(order)}</small></td><td data-label="Status"><StatusBadge status={order.status}/></td><td data-label="Total">{money(orderTotal(order))}</td><td data-label="Delivery">{order.deliveryOption||'—'}</td><td data-label="Created">{dateTime(order.createdAt)}</td><td><div className="adm-actions"><button className="adm-icon-btn"><Icon name="arrow"/></button></div></td></tr>)}</tbody></table></div><div className="adm-pagination"><span>{data.meta?.total||rows.length} orders · page {page}</span><div><button disabled={page<=1} onClick={()=>setPage(p=>p-1)}>←</button><button disabled={page>=(data.meta?.totalPages||1)} onClick={()=>setPage(p=>p+1)}>→</button></div></div></>}</section>{selected&&<OrderDrawer id={selected} onClose={()=>setSelected(null)} onSaved={m=>flash(m)} onError={m=>flash(m,'error')}/>} {toast&&<Toast {...toast} onClose={()=>setToast(null)}/>}</AdminShell>}
